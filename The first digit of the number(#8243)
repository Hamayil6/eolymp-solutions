#include <stdio.h>
#include <stdlib.h>

int main() {
    long long n,digit;
    scanf("%lld", &n);
    n=abs(n);
    while(n>0){
        digit=n%10;
        n=n/10;
    }
    printf("%lld", digit);
    return 0;
}
