#include <stdio.h>

int main() {
    long long n,m;
    scanf("%lld", &n);
    if(n==0){
        printf("0");
    }
    while(n>0){
        m=n%10;
        printf("%lld", m);
        n=n/10;
    }
    return 0;
}
