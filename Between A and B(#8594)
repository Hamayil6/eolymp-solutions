#include <stdio.h>
int main() {
    unsigned long long a, b, x;
    scanf("%llu %llu %llu", &a, &b, &x);
    unsigned long long count = (b / x) - (a / x);
    if (a%x==0){
        count++;
    }
    printf("%llu\n", count);
    return 0;
}
