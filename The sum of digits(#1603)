#include <stdio.h>
#include <stdlib.h>

int main() {
   long long n,digit,sum=0;
   scanf("%lld", &n);
   n=abs(n);
   while(n>0){
       digit=n%10;
       sum=sum+digit;
       n=n/10;
   }
   printf("%lld", sum);
   
   return 0;
}
