#include <stdio.h>
#include <string.h>

int main() {
    char s[1000];
    int a, b;
    fgets(s, sizeof(s), stdin);
    scanf("%d %d", &a, &b);
    printf("%d\n", b - a + 1);
    for (int i = a - 1; i < b; i++) {
        printf("%c", s[i]);
    }
    printf("\n");

    return 0;
}
