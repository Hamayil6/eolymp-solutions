#include <stdio.h>

int main() {
    int n,m=0;
    scanf("%d", &n);
    double arr[n],a,sum=0.0;
    for(int i=0;i<n;i++){
        scanf("%lf", &a);
        if(a<0){
            sum=sum+a;
            m++;
        }
    }
    printf("%d %.2lf", m,sum);
    
    return 0;
}
