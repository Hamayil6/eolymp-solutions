#include<stdio.h>
#include<string.h>
int main(){
   int a=0, b=0;
   char s[1000];
   scanf("%s", s);
   for (int i=0; i<strlen(s); i++){
      if (s[i]=='2'){
         a++;
         continue;
      } else if (s[i]=='5'){
         b++;
         continue;
      }
   }
   if (a>b){
      printf("2");
   } else if(b>a){
      printf("5");
   } else {
      printf("=");
   }
}
