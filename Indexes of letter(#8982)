#include<stdio.h>

int main() {
   char str[1000];
   int found=0;
   fgets(str, sizeof(str), stdin);
   for(int i=0; str[i] != '\0'; i++){
      if (str[i]=='a'){
         printf("%d", i);
         found=1;
      }
   }
   if (!found){
      printf("-1");
   }
   return 0;
}
