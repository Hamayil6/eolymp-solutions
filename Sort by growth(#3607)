#include <stdio.h>

int main() {
    int len;
    while (scanf ("%d", &len) ==1) {
        int min, max;
        int heights[len];
        for (int i = 0; i < len; i++) {
            scanf("%d", &heights[i]);
        }
        scanf("%d %d", &min, &max);
        int count = 0;
        for (int i = 0; i < len; i ++) {
            if (heights[i] >= min && heights[i] <= max) {
                count++;
            }
        }
        printf("%d\n", count);
    }
    return 0;
    
}
