#include <stdio.h>
#include <math.h>

int main() {
    int n;
    scanf("%d", &n);
    double max_num=0.0,a;
    for(int i=0; i<n; i++){
        scanf("%lf", &a);
        if(fabs(a)>=max_num){
            max_num=fabs(a);
        }
    }
    printf("%.2lf", max_num);
    
    return 0;
}
