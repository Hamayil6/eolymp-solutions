#include <stdio.h>
#include <string.h>

int main() {
    char a[1000]; 
    fgets(a, sizeof(a), stdin);
    size_t len = strlen(a);
    if (len > 0 && a[len - 1] == '\n') {
        a[len - 1] = '\0';
    }
    printf("%s\n", a);
    printf("%lu\n", strlen(a));

    return 0;
}
