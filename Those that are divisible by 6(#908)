#include <stdio.h>
int main(){
    long long n,a,sum=0,b=0;
    scanf("%lld",&n);
    int array[n];
     for(int i=0;i<n;i++){
     scanf("%lld",&a);
     if(a%6==0 && a>0){
	     sum=sum+a;
	     b++;
      }	
   }
   printf("%lld %lld", b,sum);
    
    return 0; 
}
