#include <stdio.h>
#include <string.h>

int main()
{
    char s[1000];
    fgets(s, sizeof(s), stdin);
    for (int i = 0; s[i] != '\0'; i++) {
        if (s[i] == 'a') {
            continue;
        }
        printf("%c", s[i]);
    }
    printf("\n");

    return 0;
}
