#include <stdio.h>
#include <math.h>

int main() {
   int a,b,i,digit1,digit2,digit3;
   scanf("%d%d", &a,&b);
   for(i=a; i<=b ;i++){
       digit1=i/100;
       digit2=(i/10)%10;
       digit3=i%10;
       if(i==pow(digit1,3)+pow(digit2,3)+pow(digit3,3))
       printf("%d " ,i);
   }
    return 0;
}
